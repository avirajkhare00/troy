base: mlx-community/Qwen3-0.6B-4bit
task: dpo

data:
  train: ./data/train.jsonl
  format: preference
  val_split: 0.05

training:
  iters: 30
  lr: 1e-5
  batch_size: 2
  seq_len: 1024
  lora:
    r: 8
    alpha: 16
  dpo:
    beta: 0.1

output: ./output
